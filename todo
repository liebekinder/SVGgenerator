//todo : variables locales existent encore pour déclaration hors ctx
op sur le trucs (ser_x, set_y,...)
bool via float ?
math funct : cos et co.
likaga entre objets ? => le faire

ligne à ajouter pour l'affectatio d'un float : la var est t'elle un float ?